# AutoMM 全自动数学建模 Harness 需求

## 目标

构建一个以数学建模竞赛整道题为控制尺度的全自动 Agent harness。任务通常包含 3 到 5 个相互依赖的小问，最终交付一篇完整的 Markdown 论文。

## 质量标准

- 假设有文献、机理或常识依据。
- 变量、单位、公式、边界条件和约束定义一致。
- 结果满足题目约束，数量级和常识合理。
- 小问之间共享的变量、参数和结论保持一致。
- 复杂算法必须有必要性、对比实验和可解释性。
- 结果按模型适用性执行扰动、敏感性和 ablation 分析；不适用时必须记录跳过理由。
- 图表风格统一，图表可追溯到输入和脚本。
- 引用真实、完整且正文引用与参考文献表相互对应。

## 控制模式

本目录只实现全自动模式。Orchestrator 负责从题目解析到论文确认的阶段调度；不可逆的最终论文生成仍需人类邮件确认。

## 非目标

- 不使用 leaderboard 作为主要优化目标。
- 不保留评分榜导向的提交门禁、交叉验证折数或单一评估模型逻辑。
- 不把 GPU 监控作为核心监控对象。
- 不用绝对路径。
