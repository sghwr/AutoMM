---
name: ablation-study
description: 对模型假设、模块和算法策略运行消融实验。
---

# Ablation Study

先判断模型是否适用；不适用则记录 skipped 理由。

适用时，以当前完整模型为内部对照，选择 3–4 个有解释意义的公式项或约束，每个实验只改变一项，保持数据、种子和其他配置一致。任务必须有唯一 task ID。结果写入 `ablations/`，生成对照表和图，解释哪些复杂性真正贡献结论。

不要求额外外部 baseline，但不能没有完整模型对照。
